export enum Role {
  STUDENT = 'STUDENT',
  CURATOR = 'CURATOR',
  ADMIN = 'ADMIN',
}

export enum TaskStatus {
  TODO = 'TODO',
  REVIEW = 'REVIEW',
  CHANGES_REQUESTED = 'CHANGES_REQUESTED',
  DONE = 'DONE',
}
